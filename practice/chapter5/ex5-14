#!/bin/bash

# $$ -> 현재 스크립트의 PID
echo $$

# 표준 출력을 log 파일로 쓰기 변경
exec > log
ls -l /proc/self

echo END


# > 파일
# 표준 출력을 파일로 내보내며, 파일은 새로 생성

# 2> 파일
# 표준 에러를 파일로 내보내며, 파일은 새로 생성

# >> 파일
# 표준 출력을 파일로 내보내며, 파일은 기존 내용 유지, 끝에 추가

# 2>&1 파일
# 표준 에러를 표준 출력으로 내보냄 (반대도 가능)